services:
  - type: web
    name: x-bot-md
    env: docker
    branch: main
    repo: https://github.com/A-S-W-I-N-S-P-A-R-K-Y/X-BOT-MD.git
    dockerfilePath: Dockerfile
    plan: free
    autoDeploy: false
    envVars:
      - key: SESSION_ID
        sync: false
      - key: DATABASE_URL
        sync: false
      - key: SUDO
        sync: false
